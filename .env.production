# Solar Monitor - Production Environment Variables
# IMPORTANT: Copy this file to .env and fill in secure values
# NEVER commit .env to version control!

# Database
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# JWT Authentication
JWT_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_32

# PgAdmin (optional)
PGADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# Generate secure values with:
# openssl rand -base64 32  (for JWT_SECRET)
# openssl rand -base64 16 | tr -dc 'a-zA-Z0-9' | head -c 20  (for passwords)
